[gamevals.inv]
# Persisted holding inventory for duel stakes (see .data/raw-cache/server/inv.toml).
# Kept in the fork's reserved 63k range: upstream's motherlode mine claims 1101.
emirs_arena_stake = 63520
