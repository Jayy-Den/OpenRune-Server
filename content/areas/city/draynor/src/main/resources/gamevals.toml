[gamevals.timer]
# Per-cycle burn timer for the candle hazard in Draynor Manor's basement. The timer map keys
# timers as shorts, so this lives in the fork's reserved 30000+ timer band (fits a short, and
# upstream timer ids never grow into it — see docs/plugins.md).
draynor_candles = 30002
