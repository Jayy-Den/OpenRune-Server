# Game values
# You want to edit these to fit your game.
name: "OpenRune"
game-port: 43594
revision: 240.2
environment: "LIVE"
world: 255

gameplay:
  quest-requirements:
    # Temporary core-mechanics default. Use "respect-progress" when quest gates should use real
    # quest state, or "virtual-completions" for league-style profiles.
    mode: "assume-completed"
    virtual-completions: []
    virtual-lines: []

central:
  same-instance: true
  host: "127.0.0.1"
  link-port: 9091
  postgres:
    pool-size: 10


# Admin QA helpers. When enabled, ::ifopenpersist opens modals that the server will not
    # auto-close, which is useful for agent-driven UI tests that need to OCR/inspect a modal
    # without racing the client-side 30-60s modal lifetime.
    #ifopen-persist-enabled: false

# Central server connection settings (used when connecting to a different instance)
    #central:
    #same-instance: false
    #host: "127.0.0.1"
    #link-port: 9091
    #world-key: "Ae2fPNJIjrvNKepBebm_qakFc5nBW4o7"
    #postgres:
    # PostgreSQL JDBC connection URL
    #jdbc-url: "jdbc:postgresql://127.0.0.1:5432/openrune_central"
    # Database username
    #user: "postgres"
    # Database password
    #password: "-"
    # Maximum number of connections in the pool
    #pool-size: 10
